<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lut.cn.c2cplatform.mapper.CartItemMapper">

    <resultMap id="CartItemResultMap" type="lut.cn.c2cplatform.entity.CartItem">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="lut.cn.c2cplatform.entity.CartItem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO shopping_cart (user_id, product_id, quantity, created_at, updated_at)
        VALUES (#{userId}, #{productId}, #{quantity}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="lut.cn.c2cplatform.entity.CartItem">
        UPDATE shopping_cart
        SET quantity = #{quantity}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM shopping_cart WHERE id = #{id}
    </delete>

    <delete id="deleteByUserIdAndProductId">
        DELETE FROM shopping_cart
        WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>

    <select id="selectByUserIdAndProductId" resultMap="CartItemResultMap">
        SELECT * FROM shopping_cart
        WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <select id="selectByUserId" resultMap="CartItemResultMap">
        SELECT * FROM shopping_cart
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM shopping_cart
        WHERE user_id = #{userId}
    </select>

    <delete id="deleteByUserId">
        DELETE FROM shopping_cart WHERE user_id = #{userId}
    </delete>

</mapper>

